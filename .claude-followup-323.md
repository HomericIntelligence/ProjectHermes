{
  "follow_ups": [
    {
      "category": "core",
      "title": "ADR-001 Decision section still describes restart-only stance",
      "body": "ADR-001:29-40 (Decision section) states 'Reconnection to NATS is handled only at startup' and describes a 4-step sequence ending with 'The operator is alerted and must restart Hermes or restore NATS connectivity.' This contradicts ADR-002 which documents the runtime _reconnect_loop. Update the Decision section to note that both startup retries AND runtime reconnection via the external loop are now in place, with the loop handling transient drops and startup retries handling boot-time coordination."
    },
    {
      "category": "core",
      "title": "ADR-001 Rationale section references outdated startup-only design",
      "body": "ADR-001:52-54 (Rationale section) claims 'The most frequent reason Hermes cannot reach NATS is that both services are starting simultaneously... Startup retries handle this without compromising the runtime contract.' This rationale assumes no runtime reconnection exists. Update to acknowledge that startup retries handle boot-time synchronization while the runtime loop (documented in ADR-002) handles operational resilience for transient drops."
    }
  ],
  "rejected": [
    {
      "title": "Add integration test for NATS restart recovery",
      "reason": "Integration test for runtime reconnect already exists at tests/test_integration.py:773-780 (test_reconnect_after_real_nats_restart). Adding more integration tests falls outside the scope of this documentation fix."
    },
    {
      "title": "Document reconnect loop in code comments",
      "reason": "Code comments at publisher.py:146-154 already document the callback design and the existence of _reconnect_loop. This is documentation polish, not a core defect."
    },
    {
      "title": "Add examples to CLAUDE.md about reconnect configuration",
      "reason": "Configuration examples are nice-to-have documentation enhancements, not core functionality defects or concrete bugs."
    }
  ]
}
